<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sciencecenter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Attendance_Manage">

        </activity>
        <activity android:name=".ClassInside" />
        <activity android:name=".NoticeFeedback" />
        <activity android:name=".register" />
        <activity android:name=".Profile" />
        <activity android:name=".Payment" />
        <activity android:name=".Attendance" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".Notices" />
        <activity android:name=".Login" />
        <activity android:name=".Home" />
    </application>

    <uses-feature
        android:name="android.hardware.Camera"
        android:required="true" />

</manifest>